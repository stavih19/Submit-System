import { AdvencedConfiguration } from "./advenced-configuration";

export interface AutoCheck {
    name: string;
    input: string;
    output: string;
    advencedConfiguration: AdvencedConfiguration;
}