export interface CheckerExInfo {
    forEach(arg0: (element: any) => void);
    exID: string;
    exName: string;
    courseName: string;
    courseNumber: number;
    toCheck: number;
    appeals: number;
}